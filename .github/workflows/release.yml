name: Release

on:
  push:
    branches:
      - main
      - master

jobs:
  release:
    runs-on: ubuntu-latest

    # Only run when commit message starts with "Release:"
    if: startsWith(github.event.head_commit.message, 'Release:')

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract version from plugin header
        id: version
        run: |
          VERSION=$(grep -oP "Version:\s*\K[0-9]+\.[0-9]+\.[0-9]+" fluent-cart-simulator.php)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Detected version: $VERSION"

      - name: Install Composer dependencies
        run: composer install --no-dev --optimize-autoloader --no-interaction

      - name: Build zip
        run: |
          mkdir -p /tmp/fluent-cart-simulator
          rsync -a --exclude='.git' \
                   --exclude='.github' \
                   --exclude='.gitignore' \
                   --exclude='composer.lock' \
                   --exclude='.plugshipignore' \
                   --exclude='node_modules' \
            ./ /tmp/fluent-cart-simulator/
          cd /tmp && zip -r fluent-cart-simulator.zip fluent-cart-simulator/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: ${{ steps.version.outputs.version }}
          body: "Release ${{ steps.version.outputs.version }}"
          files: /tmp/fluent-cart-simulator.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
